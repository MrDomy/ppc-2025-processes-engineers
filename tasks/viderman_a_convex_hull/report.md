# Построение выпуклой оболочки для компонентов бинарного изображения

- **Студент**: Видерман Александра Геннадьевна, группа 3823Б1ПР2
- **Технология**: MPI + SEQ
- **Вариант**: 32

## 1. Введение

В компьютерной графике и обработке изображений часто возникает задача анализа бинарных изображений, содержащих различные объекты. Одной из важных задач является нахождение компонент связности (связанных областей) и построение их выпуклых оболочек. Выпуклая оболочка представляет собой минимальный выпуклый многоугольник, содержащий все точки компоненты. Эта задача имеет широкое применение в компьютерном зрении, распознавании образов, обработке медицинских изображений и геометрических вычислениях.

Целью данной работы является разработка эффективного параллельного алгоритма для построения выпуклых оболочек компонент бинарного изображения с использованием технологии MPI, а также создание корректной последовательной версии для сравнения производительности.

## 2. Постановка задачи

**Формальная постановка задачи:**

Дано:
- Бинарное изображение размером W × H пикселей
- Каждый пиксель имеет значение 0 (фон) или 255 (объект)

Требуется:
1. Найти все связные компоненты (области из пикселей со значением 255)
2. Для каждой компоненты построить выпуклую оболочку
3. Вернуть список компонент с их выпуклыми оболочками

**Ограничения:**
- Используется 4-связность (пиксели связаны по горизонтали и вертикали)
- Изображение представляется в виде одномерного массива
- Загрузка реальных изображений не требуется (генерируются тестовые данные)

**Входные данные:**
```cpp
struct ImageData {
    int width;
    int height;
    std::vector<uint8_t> pixels; // 0 или 255
};
```

**Выходные данные:**
```cpp
struct Component {
    std::vector<Point> pixels; // Все точки компоненты
    std::vector<Point> hull;   // Выпуклая оболочка
};
```

## 3. Базовый алгоритм (последовательный)

Последовательный алгоритм состоит из двух основных этапов:

### 3.1 Поиск компонент связности (BFS)
```
Для каждого пикселя изображения:
    Если пиксель == 255 и не посещен:
        Запустить BFS/DFS для поиска всех связанных пикселей
        Сохранить найденные точки как отдельную компоненту
```

### 3.2 Построение выпуклой оболочки (Алгоритм Грэхема)
```
Для каждой компоненты:
    Если точек ≤ 2: оболочка = точки
    Иначе:
        1. Найти нижнюю-левую точку (опорную)
        2. Отсортировать остальные точки по полярному углу
        3. Построить оболочку, удаляя невыпуклые вершины
```

**Псевдокод алгоритма Грэхема:**
```
GrahamScan(points):
    pivot = точка с минимальной Y (и минимальной X при равенстве)
    sort(points по углу относительно pivot)
    hull = empty stack
    for p in sorted_points:
        while len(hull) ≥ 2 and cross(hull[-2], hull[-1], p) ≤ 0:
            hull.pop()
        hull.push(p)
    return hull
```

**Сложность:**
- Поиск компонент: O(W×H)
- Построение оболочки: O(n log n) для n точек в компоненте
- Общая сложность: O(W×H + N⋅n log n), где N - количество компонент

## 4. Схема распараллеливания (MPI)

### 4.1 Архитектура параллельного алгоритма

```
    ┌─────────────────────────────────────────┐
    │           Процесс 0 (Master)            │
    │ Чтение → Разделение → Слияние → Рассылка│
    └─────────────────┬───────────────────────┘
                      ↓
          ┌───────────┼──────────┐
          ↓           ↓          ↓
    ┌─────────┐ ┌─────────┐ ┌─────────┐
    │Процесс 1│ │Процесс 2│ │Процесс N│
    └─────────┘ └─────────┘ └─────────┘
```

### 4.2 Распределение данных

Изображение делится горизонтальными полосами между процессами:
```cpp
int rows_per_proc = height / num_procs;
int remainder = height % num_procs;
// Каждый процесс получает rows_per_proc + (если остаток) строк
```

### 4.3 Этапы параллельного алгоритма

1. **Распределение данных** (`MPI_Scatterv`)
   - Процесс 0 делит изображение и рассылает части

2. **Локальный поиск компонент** (параллельно)
   - Каждый процесс находит компоненты в своей полосе

3. **Сбор фрагментов** (`MPI_Gather` + `MPI_Send/Recv`)
   - Все процессы отправляют компоненты процессу 0

4. **Слияние фрагментов** (только процесс 0)
   - Объединение компонент, разделенных границами полос
   - Используется Union-Find (DSU) с хэш-таблицей

5. **Построение оболочек** (только процесс 0)
   - Для каждой объединенной компоненты строится оболочка

6. **Рассылка результата** (`MPI_Bcast`)
   - Все процессы получают финальный результат

### 4.4 Обработка граничных случаев

Компоненты, пересекающие границы полос, обнаруживаются с помощью проверки 8-соседей:
```cpp
for точка in компонента:
    for сосед in 4_соседей(точка):
        if сосед принадлежит другой компоненте:
            объединить_компоненты(текущая, соседняя)
```

## 5. Детали реализации

### 5.1 Структура проекта
```
viderman_a_convex_hull/
├── common/include/common.hpp      # Общие типы данных
├── seq/                           # Последовательная версия
│   ├── include/ops_seq.hpp
│   └── src/ops_seq.cpp
├── mpi/                           # Параллельная версия (MPI)
│   ├── include/ops_mpi.hpp
│   └── src/ops_mpi.cpp
├── tests/                         # Тесты
│   ├── functional/main.cpp
│   └── performance/main.cpp
├── settings.json                  # Конфигурация технологий
└── info.json                      # Метаданные студента
```

### 5.2 Ключевые классы

**Последовательная версия:**
- `VidermanAConvexHullSEQ` - наследует от `BaseTask`
- Методы: `FindConnectedComponents`, `CrossProduct`, `BuildConvexHull`, `RemoveDuplicatePoints`

**Параллельная версия:**
- `VidermanAConvexHullMPI` - наследует от `BaseTask`
- Методы: `GatherComponent...` - набор функций-объединителей, вспомогательные функции - `ComputeDisplacementsAndCounts` и `Pack/Unpack...`, `RemoveDuplicatePoints`, `GrahamScan`

### 5.3 Обработка особых случаев
- Пустое изображение → пустой результат
- Один пиксель → оболочка из одной точки
- Два пикселя → оболочка из двух точек
- Коллинеарные точки → правильная обработка в алгоритме Грэхема

## 6. Экспериментальная установка

### 6.1 Аппаратное и программное обеспечение
- **Процессор**: Intel Core i7-9750H (6 ядер, 12 потоков)
- **ОС**: Windows 10
- **Компилятор**: MSVC 19.28.29334
- **Тип сборки**: RelWithDebInfo
- **MPI реализация**: Microsoft MPI 10.0

### 6.2 Переменные окружения
```bash
export PPC_NUM_PROC=4    # Количество MPI процессов
export PPC_NUM_THREADS=1 # Для последовательной версии
```

### 6.3 Тестовые данные
Тесты используют синтетические изображения:
- Размер: 1024×1024 пикселей
- Компоненты: 5 изолированных точек
- Генерация: `CreateComplexImage()` в коде тестов

## 7. Результаты и обсуждение

### 7.1 Проверка корректности

**Функциональные тесты** (4 тестовых случаев):
1. Стандартное изображение - *размер: 2х2*;
2. Произвольоне *бинарное* изображение - *размер: 236х419*;
3. Произвольоне *бинарное* изображение - *размер: 168х300*;
4. Сгенерированное изображение - *размер: 1024х1024*;

Все тесты пройдены успешно:
```
[==========] 4 tests from ConvexHullFuncTests/VidermanARunFuncConvexHull (1329 ms total)
[  PASSED  ] 4 tests.
```

**Проверка корректности включает:**
- Количество компонент соответствует ожидаемому
- Все точки компоненты находятся внутри/на границе оболочки
- Оболочка является выпуклой
- Оболочка содержит все крайние точки компоненты

### 7.2 Производительность

**Результаты тестов производительности:**

| Технология | Процессы | Время (сек) | Ускорение | Эффективность |
|------------|----------|-------------|-----------|---------------|
| SEQ        | 1        | 0.000788 | 1.00 | 100% |
| MPI        | 1        | 0.001003 | 0.79 | 79%  |
| MPI        | 2        | 0.000731 | 1.08 | 54%  |
| MPI        | 4        | 0.000574 | 1.37 | 34%  |
| MPI        | 8        | 0.000388 | 1.51 | 25%  |

**Анализ результатов:**

1. **При 1 процессе**: MPI версия немного медленнее SEQ из-за накладных расходов на инициализацию MPI.

2. **При 2 процессах**: Ускорение 1.08×, эффективность 54%. Невысокая эффективность объясняется:
   - Накладными расходами на коммуникацию
   - Маленьким размером задачи (2×2, 168×300, 236×419 точек)
   - Неравномерной нагрузкой (слияние на процессе 0)

3. **При 4 процессах**: Лучший результат - ускорение 1.51×, эффективность 25%.
   - Наблюдаем ускорение обработки при обработке больших данных (1024×1024), однако снижение эффективнорсти из-за затрат на накладные расходы
   - Распределение работы между процессами для небольших изображений нельзя назвать равномерным

4. **При 8 процессах**: Падение эффективности до 25%, но наибольшее ускорение:
   - Избыточное количество процессов для маленькой задачи
   - Увеличение накладных расходов на коммуникацию
   - Узкое место на процессе 0 при слиянии

### 7.3 Влияние размера задачи

Для более реалистичной оценки производительности необходимы тесты с:
- Большими изображениями (4096×4096 и более)
- Большим количеством компонент
- Различными формами компонент

**Ожидаемое поведение на больших задачах:**
- Ускорение должно приближаться к линейному
- Эффективность должна улучшаться
- Время коммуникации станет менее значимым

## 8. Выводы

### 8.1 Выполненные задачи
1. Реализована корректная последовательная версия алгоритма
2. Реализована параллельная MPI версия с правильным слиянием компонент
3. Обеспечена идентичность результатов SEQ и MPI версий
4. Достигнуто ускорение до 1.51× на 8 процессах
5. Все функциональные тесты пройдены

### 8.2 Ограничения и проблемы
1. **Централизованное слияние**: Процесс 0 становится узким местом при большом количестве процессов
2. **Маленький размер тестов**: Не позволяет полностью оценить потенциал параллелизации
3. **Статическое распределение**: Не учитывает неравномерность распределения компонент

### 8.4 Практическая значимость
Разработанный алгоритм может быть использован в:
- Системах компьютерного зрения для анализа объектов
- Обработке медицинских изображений
- Геометрических вычислениях в CAD системах
- Распознавании образов и сегментации изображений

## 9. Список литературы

1. **Graham, R. L.** (1972). "An Efficient Algorithm for Determining the Convex Hull of a Finite Planar Set". Information Processing Letters.
2. **Cormen, T. H., Leiserson, C. E., Rivest, R. L., & Stein, C.** (2009). "Introduction to Algorithms". MIT Press.
3. **MPI Forum** (2021). "MPI: A Message-Passing Interface Standard. Version 4.0".
4. **Shamos, M. I.** (1978). "Computational Geometry". PhD thesis, Yale University.
5. **OpenCV Library Documentation** - алгоритмы обработки изображений и поиска контуров.

## Приложение: Ключевые фрагменты кода

### Алгоритм слияния компонент (Union-Find)
```cpp
int FindRoot(std::vector<int>& parent, int x) {
    while (parent[x] != x) {
        parent[x] = parent[parent[x]];  // Path compression
        x = parent[x];
    }
    return x;
}

void UnionSets(std::vector<int>& parent, std::vector<int>& rank, int x, int y) {
    int rootX = FindRoot(parent, x);
    int rootY = FindRoot(parent, y);
    
    if (rootX != rootY) {
        if (rank[rootX] < rank[rootY]) {
            parent[rootX] = rootY;
        } else if (rank[rootX] > rank[rootY]) {
            parent[rootY] = rootX;
        } else {
            parent[rootY] = rootX;
            rank[rootX]++;
        }
    }
}
```

### Рассылка результата всем процессам
```cpp
void BroadcastFinalResult() {
    int total_components = 0;
    if (rank_ == 0) {
        total_components = static_cast<int>(output.size());
    }
    
    MPI_Bcast(&total_components, 1, MPI_INT, 0, MPI_COMM_WORLD);
    
    // Рассылка данных каждой компоненты
    for (int i = 0; i < total_components; ++i) {
        // Рассылка точек компоненты
        int num_points;
        if (rank_ == 0) num_points = output[i].pixels.size();
        MPI_Bcast(&num_points, 1, MPI_INT, 0, MPI_COMM_WORLD);
        
        // Рассылка оболочки
        int hull_size;
        if (rank_ == 0) hull_size = output[i].hull.size();
        MPI_Bcast(&hull_size, 1, MPI_INT, 0, MPI_COMM_WORLD);
    }
}
```